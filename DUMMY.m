clc
  load census;

   z = [1:7]
%f=fit(cdate,pop,'poly2')
cdate
pop
plot(f,cdate,pop)
  for i=1:6
     x(i,1) = -i;
     y(i,1) = -i^3;
 end
 x(5,1) = 100
 y
 className = class(x)  
   %Fit2=fit(x,y,'poly3','Normalize','off')
   plot(f)
  
  